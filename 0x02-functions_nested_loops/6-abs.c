#include "main.h"

/**
 * _abs - function that computes the absolute value of an integer
 * @r: the variable
 * Return: absolute value
 **/

int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (-r);
	}
}
