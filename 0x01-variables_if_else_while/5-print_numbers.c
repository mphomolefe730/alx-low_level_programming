#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 *
 **/

int main(void)
{
	int number = 0;

	/*function*/	
	while (number < 9)
	{
		printf("%d", number);
		number++;
	}
	return (0);
}
