#include "main.h"

/**
 * _isupper - checks for upper case charactors
 *
 * @c: The variable the computer put
 *
 **/

int _isupper(int c)
{
	char = c;

	if (c >= 'A' || c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
